# VERITAS joint run operator card — 2026-09-23

This is the operator card for window B. It does not replace the byte-agreed artifacts in this
directory.

## HARD STOP discovered in final preflight

Do not issue or send a live gate pair until the Execution Receipt path below is resolved.

- The immutable VERITAS r6 policy accepts ExecutionReceipt v2/v3/v4 and explicitly says v5 is not
  activated implicitly.
- The current production issuer at `https://www.oracleinsight.xyz` publishes and issues v5 only.
- The historical 2026-09-08 v4 deployment still exists, but its Vercel URL is protected by SSO and
  is not an operator-usable endpoint. Its signer has not been proven current.
- The only local production signer resolves to `0xa268…e419`; the current public registry says that
  key expired on 2026-09-02. Never use it to sign a new v4 receipt.

The gate operator now preflights the configured Execution Receipt issuer before making either live
gate request. It requires current schema v4 and a currently authorised non-sample registry signer.
With the default production URL it intentionally refuses because the issuer reports v5. A URL must
not be passed merely to bypass this control: it must be an agreed, reachable v4 production issuer
whose returned signer passes the live registry check. If this is not available before the window,
the funded attempt is `NO_START`; an explicitly agreed abort-only rehearsal cannot close F12.

## Fixed controls

- Window: `2026-09-23 12:00–13:00 UTC` / `20:00–21:00 Asia/Shanghai`.
- Run id: `insight-veritas-2026-09-18` exactly. Do not rename it for the second window.
- Attempts: `3, 4, 5, 6, 7`. Do not restart at 1.
- Attempt 3 requires both an open window and a complete window-specific `VERITAS_READY`.
- Attempts 4–7 each require a separate explicit `VERITAS_RETRY_REQUEST`.
- Target times `12:11`, `12:22`, `12:33`, and `12:44` UTC are not authorization.
- No pre-signing and no reuse. Every attempt gets a new pair.
- Stop requesting gates at minute 44, stop Bitcoin broadcasts after minute 47, conclude attempts
  by minute 55, and publish records by minute 60.

## Before 19:55 Asia/Shanghai

1. Keep the existing VERITAS email thread open. It is the authoritative channel.
2. Export a valid Insight API key in the terminal without putting it in shell history:

   ```sh
   read -s INSIGHT_API_KEY
   export INSIGHT_API_KEY
   ```

3. Run the offline checks:

   ```sh
   node scripts/veritas-joint-run/verify-veritas-window-2-plan.mjs
   node scripts/veritas-joint-run/verify-veritas-round9-run-readiness.mjs
   node scripts/veritas-joint-run/verify-veritas-round8-publication-closure.mjs
   node scripts/veritas-joint-run/verify-veritas-round7-closure.mjs
   npm run verify:vrt1
   ```

4. Confirm the public registry has an unrevoked non-sample key valid now. Do not use the local
   `.env.local` signer: it resolves to `0xa268…e419`, whose published validity ended on 2026-09-02.
   The live operator calls the production service and refuses any response outside the current
   registry window.
5. Resolve the v4 Execution Receipt issuer hard stop above. Record its HTTPS origin. Do not accept a
   v5 endpoint, sample signer, expired signer, Vercel login redirect, or an endpoint that the other
   party has not agreed for this run.

## Review `VERITAS_READY` around 19:55

Do not sign yet. The message must be window-specific and contain only fresh 2026-09-23 values:

- exact run id and attempt 3;
- selection rule hash `0x545ede509529b6d8716be4f74e3e6715d92d821d18493dbc7f32e15156d2fc7a`;
- Bitcoin output, confirmation and fee check;
- Ethereum balance, nonce and base-fee check;
- settlement-read endpoint agreement;
- transaction-send endpoint status;
- both chains' current state;
- minute 44 / 47 / 55 / 60 cutoffs;
- fail-closed if readers disagree or no usable source exists.

An ordinary confirmation, a schedule target, or any READY from the first window is invalid.

## Attempt 3 at or after 20:00

Replace the receipt timestamp with the time the READY reached the inbox. The command refuses to run
before 12:00 UTC, after the gate cutoff, with a different run id, or with the wrong authorization
type.

```sh
node --import tsx scripts/veritas-joint-run/prepare-live-gate-pair.mts \
  --attempt 3 \
  --authorization VERITAS_READY \
  --authorization-received-at 2026-09-23T11:55:00Z \
  --confirm-run-id insight-veritas-2026-09-18 \
  --execution-receipt-base-url https://agreed-v4-issuer.example
```

The command performs the two production v3 calls in parallel, verifies both EIP-712 signatures,
checks the attester against the live public registry, checks role/amount/600-second semantics,
computes the ordered UID hash, runs the existing renderer, and runs the pinned commitment builder.
It refuses if fewer than 540 seconds remain.

Immediately paste the complete path printed as `message=...` into the existing email thread. Send
it as plain text with no outer JSON wrapper and no attachment. Do not edit the generated body.

## Attempts 4–7

Run only after a distinct explicit retry request for that attempt. Record the actual inbox receipt
time; the operator refuses a retry request older than five minutes.

```sh
node --import tsx scripts/veritas-joint-run/prepare-live-gate-pair.mts \
  --attempt 4 \
  --authorization VERITAS_RETRY_REQUEST \
  --authorization-received-at 2026-09-23T12:11:00Z \
  --confirm-run-id insight-veritas-2026-09-18 \
  --execution-receipt-base-url https://agreed-v4-issuer.example
```

Repeat with attempts 5, 6, and 7 only when each matching request arrives. Never use the target time
as the receipt time unless a real request arrived then.

## After each send

- Record the actual email send time and the earlier of the two `validUntil` values.
- Await `BTC_BROADCAST` or `ATTEMPT_ABORT`; do not issue the next pair on your own.
- For `BTC_BROADCAST`, compare the inline canonical preimage, 409-byte length, tagged leaf, single
  leaf Merkle root, epoch, leaf count and 49-byte OP_RETURN with the generated commitment and the
  public transaction.
- Bitcoin confirmation must occur before gate expiry. A later confirmation does not revive a gate.
- Continue only after `BTC_CONFIRMED`, then verify the Ethereum commitment is in block `E` and the
  selected candidate is in a strictly later block.
- If the attempt aborts, preserve every Bitcoin and Ethereum txid, including transactions that
  confirm after expiry.

## Completion branch

When VERITAS reports the canonical first qualifying candidate, independently verify the selection,
then issue an ExecutionReceipt v4 only through the same preflighted issuer. Do not call the current
v5-only production issuer and do not use the expired local signer. Do not issue the receipt before
the candidate and ordering commitment have been independently checked. Preserve the exact
receipt-adjacent registry snapshot required by r6, including its full SHA-256 and byte length.
Return the complete receipt envelope in the same thread so VERITAS can assemble the private
email-safe final package. Recheck both chains' canonicality before accepting that package.

F12 closes only after Bitcoin confirmation, Ethereum ordering, settlement selection, a verified
Execution Receipt and the final package are all complete. The run is still not a VERITAS product
integration, verification is not endorsement, and anchoring does not prove the Insight verdict or
extend gate freshness.
