# Tasks

## 🔴 高优先级任务

### Task 1: 修复硬编码文本国际化问题
**描述**: 修复所有硬编码的中文和英文文本，使用翻译函数
- [ ] SubTask 1.1: 修复 `Navbar.tsx` 第 185 行的 `'用户'` 硬编码
  - 添加翻译键 `common.user` 到翻译文件
  - 替换为 `t('common.user')`
- [ ] SubTask 1.2: 扫描所有组件中的硬编码文本
  - 使用脚本或手动检查所有 `.tsx` 文件
  - 记录所有发现的硬编码文本
- [ ] SubTask 1.3: 添加缺失的翻译键
  - 在 `messages/en.json` 和 `messages/zh-CN.json` 中添加
  - 确保中英文翻译完整

### Task 2: 替换占位符数据
**描述**: 移除所有硬编码的模拟数据，使用真实 API 数据或添加数据不可用提示
- [ ] SubTask 2.1: 修复 `OraclePageTemplate.tsx` 第 693 行的硬编码 APR
  - 从 API 获取真实 Staking APR 数据
  - 或添加数据不可用状态和提示
- [ ] SubTask 2.2: 识别所有模拟数据
  - 扫描代码中的硬编码数值
  - 标记需要替换的数据点
- [ ] SubTask 2.3: 为模拟数据添加标识
  - 添加 "模拟数据" 或 "Demo Data" 标签
  - 使用不同样式区分

### Task 3: 统一错误处理机制
**描述**: 建立统一的错误处理标准和组件
- [ ] SubTask 3.1: 审查现有错误处理方式
  - 列出所有错误处理实现方式
  - 评估每种方式的优缺点
- [ ] SubTask 3.2: 统一使用 `ErrorFallback` 组件
  - 替换所有直接错误文本显示
  - 确保样式一致
- [ ] SubTask 3.3: 添加全局错误边界
  - 在 `layout.tsx` 中配置
  - 处理未捕获的错误

### Task 4: 添加空状态处理
**描述**: 为所有数据展示组件添加空状态
- [ ] SubTask 4.1: 识别缺少空状态的组件
  - 检查所有数据展示组件
  - 列出需要添加空状态的组件
- [ ] SubTask 4.2: 统一使用 `EmptyState` 组件
  - 替换空白显示
  - 添加友好的提示信息
- [ ] SubTask 4.3: 添加空状态国际化
  - 为所有空状态文本添加翻译

## 🟡 中优先级任务

### Task 5: 统一颜色系统
**描述**: 建立统一的颜色规范，移除混用的颜色值
- [ ] SubTask 5.1: 审查现有颜色使用
  - 列出所有使用的颜色值
  - 分类 Tailwind 默认颜色和自定义颜色
- [ ] SubTask 5.2: 统一使用 CSS 变量
  - 将所有颜色转换为 CSS 变量
  - 更新 `globals.css` 中的颜色定义
- [ ] SubTask 5.3: 移除硬编码颜色
  - 替换所有硬编码颜色值为 CSS 变量
  - 确保状态颜色统一（green/emerald）

### Task 6: 统一圆角和阴影规范
**描述**: 决定设计风格并统一全站规范
- [ ] SubTask 6.1: 决定设计风格
  - 选择：扁平化设计（无圆角）或现代圆角设计
  - 参考 Dune Analytics 等专业平台
- [ ] SubTask 6.2: 更新 CSS 变量
  - 修改 `--radius-*` 变量
  - 更新 `--shadow-*` 变量
- [ ] SubTask 6.3: 统一组件样式
  - 替换所有 `rounded-*` 类
  - 替换所有 `shadow-*` 类

### Task 7: 统一加载状态
**描述**: 建立标准的加载状态和骨架屏规范
- [ ] SubTask 7.1: 审查现有加载状态
  - 列出所有加载状态实现
  - 评估样式一致性
- [ ] SubTask 7.2: 统一使用 `LoadingState` 组件
  - 替换 `LoadingStateEnhanced` 等变体
  - 确保样式统一
- [ ] SubTask 7.3: 建立骨架屏规范
  - 定义标准骨架屏样式
  - 创建可复用的骨架屏组件

## 🟢 低优先级任务

### Task 8: 增强可访问性
**描述**: 添加完整的 ARIA 属性和键盘导航支持
- [ ] SubTask 8.1: 审查 ARIA 属性
  - 检查所有交互组件
  - 列出缺失的 ARIA 属性
- [ ] SubTask 8.2: 添加 ARIA 属性
  - 为下拉菜单添加 `role`、`aria-expanded`
  - 为图表添加替代文本
- [ ] SubTask 8.3: 完善键盘导航
  - 确保所有组件可通过键盘操作
  - 管理焦点状态

### Task 9: 性能优化
**描述**: 统一组件懒加载和图片优化
- [ ] SubTask 9.1: 统一动态导入
  - 为所有非关键组件添加 `dynamic` 导入
  - 确保加载策略一致
- [ ] SubTask 9.2: 优化图片
  - 为所有图片添加 `sizes` 属性
  - 使用适当的图片格式

### Task 10: 完善移动端适配
**描述**: 优化复杂页面在移动端的显示
- [ ] SubTask 10.1: 优化表格移动端显示
  - 为复杂表格添加移动端专用视图
  - 使用卡片布局替代表格
- [ ] SubTask 10.2: 优化图表移动端交互
  - 添加触摸手势支持
  - 调整图表尺寸

### Task 11: 整理翻译键
**描述**: 统一翻译键命名规范
- [ ] SubTask 11.1: 审查翻译键命名
  - 列出所有翻译键
  - 识别命名不一致的地方
- [ ] SubTask 11.2: 统一命名规范
  - 统一使用 camelCase 或 kebab-case
  - 建立命名标准文档

# Task Dependencies

- Task 1 (硬编码文本) 不依赖其他任务，可立即开始
- Task 2 (占位符数据) 可能需要后端 API 支持
- Task 3 (错误处理) 建议在 Task 4 之前完成
- Task 4 (空状态) 依赖 Task 3 的错误处理机制
- Task 5-7 (中优先级) 可以并行进行
- Task 8-11 (低优先级) 建议在其他任务完成后进行
