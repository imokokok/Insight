# Checklist - 代码规范合规性检查

## TypeScript 规范
- [x] 所有枚举定义使用 `const enum`（或确认普通 enum 有合理理由）
  - **结果**: 发现 6 个枚举未使用 `const enum`，需修复
- [x] 对象形状使用 `interface` 定义
  - **结果**: 发现 17 处对象形状使用 `type` 定义，需修复
- [x] 联合类型和交叉类型使用 `type` 定义
  - **结果**: 符合规范
- [x] 无显式 `any` 类型使用
  - **结果**: 发现 46 处 `any` 类型使用，需修复
- [x] 无隐式 `any` 类型使用（TypeScript strict mode 通过）
  - **结果**: 需运行 TypeScript 编译器确认

## React 组件规范
- [x] 使用客户端 hooks 的组件正确标记 `'use client'`
  - **结果**: 符合规范，所有使用客户端 hooks 的组件都正确标记
- [x] 纯展示组件未不必要标记 `'use client'`
  - **结果**: 符合规范
- [x] Props 接口使用 `ComponentNameProps` 格式命名
  - **结果**: 符合规范
- [x] 组件 Props 正确解构并设置默认值
  - **结果**: 符合规范
- [x] 事件处理器使用 `handle + EventName` 命名
  - **结果**: 符合规范

## 导入导出规范
- [x] 导入语句按正确顺序组织（React/Next.js -> 第三方库 -> 项目内部 -> 相对导入 -> 类型导入）
  - **结果**: 多处导入顺序不规范，需修复
- [x] 使用路径别名 `@/` 而非深层相对路径
  - **结果**: 符合规范，无深层相对路径
- [x] 类型导入使用 `import type` 或 `import { type X }` 语法
  - **结果**: 发现约 85+ 处类型导入未使用 `type` 关键字，需修复
- [x] 导出使用命名导出（页面和主要组件可使用默认导出）
  - **结果**: 符合规范

## 样式规范
- [x] 使用 `cn` 函数合并 Tailwind CSS 类名
  - **结果**: 仅 4 个文件使用 `cn` 函数，196+ 个文件未使用，需改进
- [x] Tailwind CSS 类名按正确顺序组织
  - **结果**: 多处类名顺序不规范，需修复
- [x] 使用 CSS 变量定义主题色
  - **结果**: 符合规范
- [x] 响应式设计遵循移动优先原则
  - **结果**: 符合规范

## 状态管理规范
- [x] React Query Query Keys 使用工厂模式定义
  - **结果**: 部分符合，12 个文件未使用工厂模式，需修复
- [x] Query 配置正确设置 staleTime 和 gcTime
  - **结果**: 部分符合，6 个文件硬编码配置，需修复
- [x] Zustand Store 使用 devtools 和 persist 中间件（如适用）
  - **结果**: 基本符合，2 个 Store 需评估是否需要 persist
- [x] Context 使用自定义 hook 消费
  - **结果**: 符合规范

## 错误处理规范
- [x] 使用自定义错误类（AppError 及其子类）而非原生 Error
  - **结果**: 发现约 60 处使用原生 Error，需修复
- [x] API 路由正确处理错误并返回标准格式
  - **结果**: 部分符合，约 15 处 API 路由未使用标准错误格式
- [x] 异步函数使用 try-catch 处理错误
  - **结果**: 符合规范
- [x] 错误边界正确配置
  - **结果**: 符合规范

## 命名约定
- [x] 组件文件使用 PascalCase 命名
  - **结果**: 符合规范
- [x] Hook 文件使用 camelCase + use 前缀命名
  - **结果**: 符合规范
- [x] 工具函数文件使用 camelCase 命名
  - **结果**: 符合规范
- [x] 布尔值变量使用 is/has/should 前缀
  - **结果**: 发现 21+ 处布尔值变量命名不规范，需修复
- [x] 函数使用动词开头命名
  - **结果**: 符合规范
- [x] 常量使用 SCREAMING_SNAKE_CASE 命名
  - **结果**: 符合规范

## 注释规范
- [x] 重要文件包含文件头注释
  - **结果**: 15+ 个重要文件缺少文件头注释，需修复
- [x] 公共函数包含 JSDoc 文档
  - **结果**: 30+ 个公共函数缺少 JSDoc 文档，需修复
- [x] 复杂逻辑包含行内注释说明
  - **结果**: 部分符合，多处复杂逻辑缺少注释
- [x] TODO/FIXME 标记正确使用
  - **结果**: 发现 2 个 TODO 标记待处理

## API 规范
- [x] API Route 使用中间件处理认证和错误
  - **结果**: 部分符合，部分 API 路由未使用中间件
- [x] API 响应使用标准格式
  - **结果**: 符合规范
- [x] 输入验证使用 Zod 或类型守卫
  - **结果**: 符合规范
- [x] API 错误返回正确的 HTTP 状态码
  - **结果**: 符合规范

## 性能优化
- [x] 组件使用 React.memo 避免不必要重渲染（如适用）
  - **结果**: 部分组件正确使用 React.memo
- [x] 使用 useMemo 缓存计算结果
  - **结果**: 符合规范
- [x] 使用 useCallback 缓存回调函数
  - **结果**: 符合规范
- [x] 大型列表使用虚拟滚动
  - **结果**: 需评估具体场景
