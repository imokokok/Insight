# Checklist

## Cross-Chain 页面 (`src/app/[locale]/cross-chain/page.tsx`)
- [x] 色盲模式切换按钮使用 `rounded-md` (行 660)
- [x] 收藏夹下拉按钮使用 `rounded-md` (行 566)
- [x] 收藏夹下拉菜单使用 `rounded-lg` (行 601)
- [x] CSV 导出按钮使用 `rounded-md` (行 710)
- [x] JSON 导出按钮使用 `rounded-md` (行 718)
- [x] 自动刷新控制区使用 `rounded-md` (行 724)

## Cross-Chain 过滤器 (`src/app/[locale]/cross-chain/components/CrossChainFilters.tsx`)
- [x] 复选框使用 `rounded` (行 234)
- [x] 区块链选择按钮使用 `rounded-md` (行 203-217)
- [x] 重置图表按钮使用 `rounded-md` (行 263-267)
- [x] 阈值输入框使用 `rounded-md` (行 315)
- [x] ATR 乘数输入框使用 `rounded-md` (行 339)

## Skeleton 组件 (`src/components/ui/Skeleton.tsx`)
- [x] rectangular 变体使用 `rounded-md` (行 29)

## 市场概览页面 (`src/app/[locale]/market-overview/page.tsx`)
- [x] 统计卡片容器使用 `rounded-lg`

## API3 页面 (`src/app/[locale]/api3/page.tsx`)
- [x] 数据卡片使用 `rounded-lg`
- [x] 信息容器使用 `rounded-md`

## Chainlink 页面 (`src/app/[locale]/chainlink/page.tsx`)
- [x] 统计网格使用 `rounded-lg`

## Pyth Network 页面 (`src/app/[locale]/pyth-network/page.tsx`)
- [x] 搜索输入框使用 `rounded-md`
- [x] 刷新按钮使用 `rounded-md`
- [x] 统计网格使用 `rounded-lg`
- [x] 价格统计卡片使用 `rounded-lg`
- [x] 链选择卡片使用 `rounded-lg`
- [x] 类别标签使用 `rounded-md`

## WINkLink 页面 (`src/app/[locale]/winklink/page.tsx`)
- [x] 统计网格使用 `rounded-lg`

## Tellor 页面 (`src/app/[locale]/tellor/page.tsx`)
- [x] 统计网格使用 `rounded-lg`

## RedStone 页面 (`src/app/[locale]/redstone/page.tsx`)
- [x] 数据源列表使用 `rounded-lg`
- [x] 状态标签使用 `rounded-md`
- [x] 价格源列表使用 `rounded-lg`

## RedStone 风险评估面板 (`src/components/oracle/panels/RedStoneRiskAssessmentPanel.tsx`)
- [x] 风险等级标签使用 `rounded-md`
- [x] 数据源列表使用 `rounded-lg`

## CrossChain 趋势图表 (`src/components/oracle/charts/CrossChainTrendChart.tsx`)
- [x] 统计卡片使用 `rounded-lg`

## Request 趋势图表 (`src/components/oracle/charts/RequestTrendChart.tsx`)
- [x] 统计卡片使用 `rounded-lg`

## 验证
- [x] TypeScript 类型检查通过
- [x] ESLint 检查通过
- [x] 所有组件符合圆角规范
