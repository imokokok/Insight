# Checklist

## 高优先级问题
- [x] 所有预言机使用统一的数据获取模式（use*AllData hooks）
- [x] 移除所有硬编码的模拟数据，改为真实 API 调用
- [x] 条件渲染逻辑正确，不会在数据加载中显示错误页面
- [x] 错误处理机制统一

## 中优先级问题
- [x] lastUpdated 计算方式在所有页面一致
- [x] refetchAll 实现方式统一
- [x] 组件 isLoading 传递正确
- [x] 没有重复的客户端实例
- [x] API3 页面数据映射正确
- [x] 类型定义统一且无重复

## 低优先级问题
- [x] 没有未使用的变量和导入
- [ ] 翻译键完整，无缺失（需要进一步验证）
- [ ] 类型安全，无潜在的 undefined 访问错误（部分修复）
- [ ] enabled 参数正确检查（已修复）
- [ ] 移动端菜单状态管理已提取为共享逻辑（可选优化）

## 功能验证
- [ ] Chainlink 页面正常工作，所有 Tab 可切换
- [ ] Pyth 页面正常工作，所有 Tab 可切换
- [ ] API3 页面正常工作，所有 Tab 可切换
- [ ] UMA 页面正常工作，所有 Tab 可切换
- [ ] Tellor 页面正常工作，所有 Tab 可切换
- [ ] Chronicle 页面正常工作，所有 Tab 可切换
- [ ] DIA 页面正常工作，所有 Tab 可切换
- [ ] RedStone 页面正常工作，所有 Tab 可切换
- [ ] BandProtocol 页面正常工作，所有 Tab 可切换
- [ ] WINkLink 页面正常工作，所有 Tab 可切换

## 代码质量
- [ ] TypeScript 类型检查通过
- [ ] ESLint 检查通过
- [x] 无未使用的变量
- [ ] 无性能警告
- [x] 所有 hooks 遵循 React hooks 规则

## 架构验证
- [x] 所有预言机页面结构一致
- [x] 所有 hooks 返回类型一致
- [ ] 所有组件 props 类型定义完整
- [ ] 错误边界正确配置
- [x] 加载状态正确显示
