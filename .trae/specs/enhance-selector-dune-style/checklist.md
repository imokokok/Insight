# Checklist

## SegmentedControl 组件样式
- [x] 容器使用 `bg-gray-100/80` 背景和 `rounded-lg` 圆角
- [x] 选中项使用 `bg-white` + `shadow-sm` + `text-gray-900`
- [x] 未选中项使用 `text-gray-600` + `hover:text-gray-900`
- [x] 按钮有 `active:scale-[0.98]` 点击反馈
- [x] 支持 xs、sm、md、lg 四种尺寸

## DropdownSelect 组件样式
- [x] 触发按钮使用 `rounded-lg` 和精致边框
- [x] 下拉菜单使用 `rounded-xl shadow-xl border-gray-100`
- [x] 搜索框使用 `bg-gray-50 rounded-lg`，聚焦时 `bg-white`
- [x] 选项悬停时 `bg-gray-50`，选中时 `bg-blue-50 text-blue-700`
- [x] 分类标签使用底部边框指示器风格

## MultiSelect 组件样式
- [x] 样式与 SegmentedControl 保持一致

## 代码质量
- [x] 所有组件有完整的 TypeScript 类型定义
- [x] 无 lint 错误（选择器组件无新增错误）
- [x] 样式与现有项目风格兼容
