import { createRequire as __createRequire } from 'node:module'; import { fileURLToPath as __fileURLToPath } from 'node:url'; import { dirname as __pathDirname } from 'node:path'; const require = __createRequire(import.meta.url); const __filename = __fileURLToPath(import.meta.url); const __dirname = __pathDirname(__filename);
import{BLOCKCHAIN_TO_CHAIN_ID,getDefaultFactory}from"./chunk-PIGPL4JF.mjs";import{extractBaseSymbol,getActiveFeedsMap,matchesChainId}from"./chunk-A3W5YKT5.mjs";import{OracleClientError,PriceFetchError,UnsupportedSymbolError,createLogger,getAdminQueries,normalizeError}from"./chunk-CO3FNEPK.mjs";var logger=createLogger("oracle-storage"),DEFAULT_CONFIG={enabled:!0,defaultExpirationHours:24},storageConfig={...DEFAULT_CONFIG};function shouldUseDatabase(){return typeof window<"u"?!1:storageConfig.enabled}function calculateExpirationDate(hours=storageConfig.defaultExpirationHours){return new Date(Date.now()+hours*3600*1e3).toISOString()}function priceDataToRecord(priceData){return{provider:priceData.provider,symbol:priceData.symbol,chain:priceData.chain??null,price:priceData.price,timestamp:priceData.timestamp,decimals:priceData.decimals??null,confidence:priceData.confidence??null,source:priceData.source??null,verification:priceData.verification??null,ingestion_timestamp:priceData.ingestionTimestamp??null,metadata_fallback:priceData.metadataFallback??null,failure_mode:priceData.failureMode??null,signal_vector:priceData.signalVector?{...priceData.signalVector}:null,ttl:calculateExpirationDate()}}function recordToPriceData(record){return{provider:record.provider,symbol:record.symbol,chain:record.chain??void 0,price:record.price,timestamp:new Date(record.timestamp).getTime(),decimals:record.decimals??void 0,confidence:record.confidence??void 0,source:record.source??void 0,verification:record.verification??void 0,ingestionTimestamp:record.ingestion_timestamp?new Date(record.ingestion_timestamp).getTime():void 0,metadataFallback:record.metadata_fallback??void 0,failureMode:record.failure_mode??void 0,signalVector:record.signal_vector??void 0}}async function savePriceToDatabase(priceData){if(!shouldUseDatabase())return!1;try{let queries=getAdminQueries(),record=priceDataToRecord(priceData);return await queries.savePriceRecord(record)!==null}catch(error){return logger.error("Failed to save price to database",normalizeError(error)),!1}}async function getPriceFromDatabase(provider,symbol,chain){if(!shouldUseDatabase())return null;try{let record=await getAdminQueries().getLatestPrice(provider,symbol,chain);return record?recordToPriceData(record):null}catch(error){return logger.error("Failed to get price from database",normalizeError(error)),null}}async function getHistoricalPricesFromDatabase(provider,symbol,chain,period){if(!shouldUseDatabase())return null;try{let queries=getAdminQueries(),now=Date.now(),startTime=now-period*60*60*1e3,records=await queries.getPriceRecords({provider,symbol,chain,startTime,endTime:now,limit:period*4});return!records||records.length===0?null:records.map(recordToPriceData)}catch(error){return logger.error("Failed to get historical prices from database",normalizeError(error)),null}}var logger2=createLogger("databaseOperations"),PROVIDERS_SKIPPING_DB_SAVE=new Set(["chainlink","api3"]),SAVE_FAILURE_THRESHOLD=10,consecutiveSaveFailures=0,DB_CACHE_TTL_MS={dia:3e4,supra:3e4,twap:3e4,switchboard:3e4},DEFAULT_DB_CACHE_TTL_MS=3e4;function getDbCacheTtlMs(provider){return DB_CACHE_TTL_MS[provider]??DEFAULT_DB_CACHE_TTL_MS}function getPriceDataAgeMs(priceData){let refTime=priceData.ingestionTimestamp??priceData.timestamp;return!refTime||refTime<=0?Number.POSITIVE_INFINITY:Math.max(0,Date.now()-refTime)}function isDbPriceStale(priceData,provider){return getPriceDataAgeMs(priceData)>getDbCacheTtlMs(provider)}function getOracleClient(provider){return getDefaultFactory().getClient(provider)}function getTargetChainId(chain,defaultChain){return BLOCKCHAIN_TO_CHAIN_ID[chain||defaultChain]??0}var USD_EQUIVALENT_QUOTES=new Set(["USD","USDT","USDC"]);function findUsdDenominatedFeed(feedsMap,baseSymbol,chainId){let upperCasedMatch=null;for(let feed of feedsMap.values())if(!(feed.symbol.toUpperCase()!==baseSymbol||!matchesChainId(feed,chainId))){if(feed.symbol!==feed.symbol.toUpperCase())return feed;upperCasedMatch||(upperCasedMatch=feed)}if(upperCasedMatch)return upperCasedMatch;let upperCasedPair=null,pairPrefix=`${baseSymbol}/`;for(let feed of feedsMap.values()){let upper=feed.symbol.toUpperCase();if(!(!upper.startsWith(pairPrefix)||!matchesChainId(feed,chainId))&&USD_EQUIVALENT_QUOTES.has(upper.slice(pairPrefix.length))){if(feed.symbol!==feed.symbol.toUpperCase())return feed;upperCasedPair||(upperCasedPair=feed)}}return upperCasedPair}async function checkSymbolActive(provider,baseSymbol,chainId){let feedsMap=await getActiveFeedsMap(provider).catch(()=>new Map);if(feedsMap.size===0)return{supported:getOracleClient(provider).isSymbolSupported(baseSymbol,void 0),activeFeeds:[],matchedSymbol:null};let matchedSymbol=findUsdDenominatedFeed(feedsMap,baseSymbol,chainId)?.symbol??null;return matchedSymbol?{supported:!0,activeFeeds:[],matchedSymbol}:(await getOracleClient(provider)).isSymbolSupported(baseSymbol,void 0)?{supported:!0,activeFeeds:[],matchedSymbol:null}:{supported:!1,activeFeeds:Array.from(feedsMap.values()),matchedSymbol:null}}function getSupportedSymbolsForError(activeFeeds,chainId,clientFallback){if(activeFeeds.length>0){let symbols=new Set;for(let feed of activeFeeds)matchesChainId(feed,chainId)&&symbols.add(extractBaseSymbol(feed.symbol).toUpperCase());return Array.from(symbols).sort()}return clientFallback()}function recordFeedHealthFailure(provider,baseSymbol,chain){try{let queries=getAdminQueries(),client=getOracleClient(provider),chainId=getTargetChainId(chain,client.getDefaultChain());getActiveFeedsMap(provider).then(feedsMap=>{let feedChainId=chainId,feedSymbol=baseSymbol,matchedFeed=findUsdDenominatedFeed(feedsMap,baseSymbol,chainId);return matchedFeed&&(feedChainId=matchedFeed.chain_id,feedSymbol=matchedFeed.symbol),queries.updateFeedHealth(provider,feedSymbol,feedChainId,!1)}).catch(err=>{logger2.warn("Failed to record feed health failure",err instanceof Error?err:void 0)})}catch(err){logger2.warn("Failed to record feed health failure",err instanceof Error?err:void 0)}}async function fetchPriceWithDatabase(provider,symbol,chain,useDatabase,forceRefresh=!1,signal){let baseSymbol=extractBaseSymbol(symbol).toUpperCase();try{let client=await getOracleClient(provider),chainId=getTargetChainId(chain,client.getDefaultChain()),{supported,activeFeeds,matchedSymbol}=await checkSymbolActive(provider,baseSymbol,chainId);if(!supported){let supportedSymbols=getSupportedSymbolsForError(activeFeeds,chainId,()=>client.getSupportedSymbols());throw UnsupportedSymbolError.create(baseSymbol,supportedSymbols,provider)}let fetchSymbol=baseSymbol;provider==="redstone"&&matchedSymbol&&(fetchSymbol=matchedSymbol);let staleDbPrice=null;if(!forceRefresh&&useDatabase&&shouldUseDatabase()){let dbPrice=await getPriceFromDatabase(provider,fetchSymbol,chain);if(dbPrice){if(!isDbPriceStale(dbPrice,provider))return dbPrice;staleDbPrice=dbPrice}}forceRefresh&&"clearCache"in client&&typeof client.clearCache=="function"&&client.clearCache();try{let livePrice=await client.getPrice(fetchSymbol,chain,{signal});return PROVIDERS_SKIPPING_DB_SAVE.has(provider)||savePriceToDatabase(livePrice).then(()=>{consecutiveSaveFailures=0}).catch(err=>{consecutiveSaveFailures+=1,logger2.error("Failed to save price to database",err instanceof Error?err:new Error(String(err)),{provider,symbol:fetchSymbol}),consecutiveSaveFailures>=SAVE_FAILURE_THRESHOLD&&logger2.warn("Price database save has failed consecutively; check database connectivity",{provider,symbol:fetchSymbol,consecutiveFailures:consecutiveSaveFailures,threshold:SAVE_FAILURE_THRESHOLD})}),livePrice}catch(liveError){if(staleDbPrice)return recordFeedHealthFailure(provider,baseSymbol,chain),logger2.warn("Live price refresh failed; serving stale DB cache",{provider,symbol:baseSymbol,error:liveError instanceof Error?liveError.message:String(liveError)}),staleDbPrice;throw liveError}}catch(error){throw!(error instanceof UnsupportedSymbolError)&&!forceRefresh&&recordFeedHealthFailure(provider,baseSymbol,chain),error instanceof PriceFetchError||error instanceof OracleClientError?error:new PriceFetchError(`Failed to fetch price for ${baseSymbol} from ${provider}`,{provider,symbol:baseSymbol,chain,retryable:!0},error instanceof Error?error:void 0)}}async function fetchHistoricalPricesWithDatabase(provider,symbol,chain,period,useDatabase){let baseSymbol=extractBaseSymbol(symbol).toUpperCase();try{let client=await getOracleClient(provider),chainId=getTargetChainId(chain,client.getDefaultChain()),{supported,activeFeeds,matchedSymbol}=await checkSymbolActive(provider,baseSymbol,chainId);if(!supported){let supportedSymbols=getSupportedSymbolsForError(activeFeeds,chainId,()=>client.getSupportedSymbols());throw UnsupportedSymbolError.create(baseSymbol,supportedSymbols,provider)}let fetchSymbol=baseSymbol;if(provider==="redstone"&&matchedSymbol&&(fetchSymbol=matchedSymbol),useDatabase&&shouldUseDatabase()){let dbPrices=await getHistoricalPricesFromDatabase(provider,fetchSymbol,chain,period);if(dbPrices&&dbPrices.length>0)return dbPrices}return await client.getHistoricalPrices(fetchSymbol,chain,period)}catch(error){throw error instanceof PriceFetchError||error instanceof OracleClientError?error:new PriceFetchError(`Failed to fetch historical prices for ${baseSymbol} from ${provider}`,{provider,symbol:baseSymbol,chain,timestamp:Date.now(),retryable:!0},error instanceof Error?error:void 0)}}export{fetchPriceWithDatabase,fetchHistoricalPricesWithDatabase};
