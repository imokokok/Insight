import { createRequire as __createRequire } from 'node:module'; import { fileURLToPath as __fileURLToPath } from 'node:url'; import { dirname as __pathDirname } from 'node:path'; const require = __createRequire(import.meta.url); const __filename = __fileURLToPath(import.meta.url); const __dirname = __pathDirname(__filename);
async function mapWithConcurrency(items,concurrency,mapper){let limit=Math.max(1,Math.floor(concurrency)),results=new Array(items.length),cursor=0,active=0,rejected=!1;return new Promise((resolve,reject)=>{if(items.length===0){resolve(results);return}let launchNext=()=>{for(;active<limit&&cursor<items.length&&!rejected;){let currentIndex=cursor++;active++,Promise.resolve().then(()=>mapper(items[currentIndex],currentIndex)).then(value=>{rejected||(results[currentIndex]=value,active--,cursor<items.length?launchNext():active===0&&resolve(results))},error=>{rejected||(rejected=!0,reject(error))})}};launchNext()})}export{mapWithConcurrency};
