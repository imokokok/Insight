import { createRequire as __createRequire } from 'node:module'; import { fileURLToPath as __fileURLToPath } from 'node:url'; import { dirname as __pathDirname } from 'node:path'; const require = __createRequire(import.meta.url); const __filename = __fileURLToPath(import.meta.url); const __dirname = __pathDirname(__filename);
var ON_CHAIN_TRUSTED_TIMESTAMP_PROVIDERS=new Set(["chainlink","api3","supra","twap","flare","switchboard","winklink"]);function resolveOracleAgeSeconds(priceData,now=Date.now()){let age=null;if(typeof priceData.dataAge=="number"&&priceData.dataAge>=0)age=priceData.dataAge;else{let refTime=priceData.timestamp;typeof refTime=="number"&&refTime>0&&(age=Math.floor((now-refTime)/1e3))}if(age===null||!Number.isFinite(age))return null;let clamped=Math.max(0,Math.floor(age));return clamped>2147483647?2147483647:clamped}export{resolveOracleAgeSeconds};
