name: Daily Report Publish

# Generates and persists the previous day's daily report (aggregates a full
# day of snapshots across every provider into the daily_reports table).
#
# Runs the `scripts/daily-report-publish.ts` runner directly on GH Actions
# (no Vercel curl) so the job escapes Vercel's 60s serverless timeout —
# report generation can exceed the ceiling. Delegates to the same
# `runDailyReportPublish()` used by the HTTP route, so behaviour is
# identical — only the executor changes.
#
# Self-healing:
#   - The primary job retries a transient failure up to 3 times with backoff.
#   - A second scheduled run later in the day (backfill) re-publishes only if
#     the report row is still missing (--if-missing), so a dropped or very
#     late GitHub `schedule` trigger still gets the report out that day.

on:
  schedule:
    # Backfill: every day at 05:30 UTC (13:30 Beijing Time) — self-heal fallback
    - cron: '30 5 * * *'
  workflow_dispatch:
    inputs:
      dispatch_id:
        description: 'Supabase cron_dispatch_runs id'
        required: false
        type: string
      scheduled_for:
        description: 'Supabase scheduled slot (audit only)'
        required: false
        type: string

permissions:
  contents: read

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
  CRON_SECRET: ${{ secrets.CRON_SECRET }}
  SITE_URL: ${{ vars.SITE_URL || 'https://www.oracleinsight.xyz' }}
  CRON_WORKFLOW: daily-report-publish-cron.yml
  CRON_DISPATCH_ID: ${{ inputs.dispatch_id || '' }}

jobs:
  publish-report:
    name: Publish (Supabase primary)
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v5

      - name: Start run ledger
        env:
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: node scripts/cron-control.mjs start

      - name: Publish previous day's daily report (retry x3)
        run: |
          set -u
          for attempt in 1 2 3; do
            echo "[daily-report-publish] attempt ${attempt}/3"
            if node .github/cron-dist/daily-report-publish.mjs; then
              echo "[daily-report-publish] completed on attempt ${attempt}"
              exit 0
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "[daily-report-publish] attempt ${attempt} failed; retrying in $((attempt * 60))s"
              sleep "$((attempt * 60))"
            fi
          done
          echo "::error::All 3 publish attempts failed"
          exit 1

      - name: Expire the production report cache
        run: |
          set -u
          if [ -z "${CRON_SECRET}" ]; then
            echo "::error::CRON_SECRET is required to invalidate the production report cache"
            exit 1
          fi

          BASE_URL="${SITE_URL%/}"
          for attempt in 1 2 3; do
            HTTP_CODE=$(curl --silent --show-error --output "$RUNNER_TEMP/report-revalidate.json" \
              --write-out '%{http_code}' --max-time 30 --request POST \
              --header "Authorization: Bearer ${CRON_SECRET}" \
              "${BASE_URL}/api/cron/daily-report/revalidate" || true)
            if [ "${HTTP_CODE}" = "200" ]; then
              echo "[daily-report-publish] production report cache expired"
              exit 0
            fi
            echo "[daily-report-publish] cache invalidation attempt ${attempt}/3 failed (HTTP ${HTTP_CODE})"
            if [ "$attempt" -lt 3 ]; then sleep "$((attempt * 10))"; fi
          done
          echo "::error::Daily report was persisted, but production cache invalidation failed"
          exit 1

      - name: Finish run ledger
        if: always()
        env:
          CRON_CONCLUSION: ${{ job.status }}
        run: node scripts/cron-control.mjs finish

  backfill-report:
    name: Backfill (05:30 UTC, if missing)
    if: github.event.schedule == '30 5 * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - name: Start fallback ledger
        env:
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: node scripts/cron-control.mjs start

      - name: Publish previous day's report only if missing
        run: node .github/cron-dist/daily-report-publish.mjs --if-missing

      - name: Expire the production report cache
        run: |
          set -u
          if [ -z "${CRON_SECRET}" ]; then
            echo "::error::CRON_SECRET is required to invalidate the production report cache"
            exit 1
          fi

          BASE_URL="${SITE_URL%/}"
          for attempt in 1 2 3; do
            HTTP_CODE=$(curl --silent --show-error --output "$RUNNER_TEMP/report-revalidate.json" \
              --write-out '%{http_code}' --max-time 30 --request POST \
              --header "Authorization: Bearer ${CRON_SECRET}" \
              "${BASE_URL}/api/cron/daily-report/revalidate" || true)
            if [ "${HTTP_CODE}" = "200" ]; then
              echo "[daily-report-publish] production report cache expired"
              exit 0
            fi
            echo "[daily-report-publish] cache invalidation attempt ${attempt}/3 failed (HTTP ${HTTP_CODE})"
            if [ "$attempt" -lt 3 ]; then sleep "$((attempt * 10))"; fi
          done
          echo "::error::Daily report was persisted, but production cache invalidation failed"
          exit 1

      - name: Finish fallback ledger
        if: always()
        env:
          CRON_CONCLUSION: ${{ job.status }}
        run: node scripts/cron-control.mjs finish
