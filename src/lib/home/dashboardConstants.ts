/**
 * Assets rendered by the homepage dashboard.
 *
 * Keep this module dependency-free: it is imported by a Client Component and
 * must not pull the server-side oracle factory into the browser bundle.
 */
export const DASHBOARD_ASSETS = ['BTC', 'ETH', 'USDT', 'SOL'] as const;
